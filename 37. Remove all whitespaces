using System;

class Program
{
    static void Main()
    {
        // Input the string with whitespaces
        Console.Write("Enter a string: ");
        string input = Console.ReadLine();

        // Remove all white spaces
        string result = input.Replace(" ", "");

        // Output the result
        Console.WriteLine("String without white spaces: " + result);
    }
}
